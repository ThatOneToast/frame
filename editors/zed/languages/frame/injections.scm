; Frame source does not inject other languages.
; The Frame Zed extension intentionally registers the LSP for `.frame` files
; only, so it does not conflict with Svelte/CSS completions inside `.svelte`.
